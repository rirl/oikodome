[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kios"
version = "0.0.1"
description = "KIOS MVP"
authors = [{name = "Richard Landon", email = "rlandon@usa.net"}]

[project.scripts]
kios = "kios.cli:main"
